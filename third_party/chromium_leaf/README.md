# Chromium Leaf FFI

See `README` in GN: enable with `enable_chromium_leaf=true` in args.gn.

Upstream Leaf: https://github.com/eycorsican/leaf (optional clone to `third_party/leaf_src` for development).

Optional standalone Cargo harness for the vendored leaf crate: see cargo_scratch/README.md.
